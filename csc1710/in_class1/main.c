#include<stdio.h>
#include"freq.h"

void init (int x [], int n)

int main (void)
{

   int lgFreq[6];
   int (lgFreq, 6);
   scanlg (lgFreq); 
   fprintf(stdout, "Freq(lgFreq)";
      return 0;
}

void int(int x [], int n)
{
   int i;
   for(i = 0; i < n; i++)
      x[i] = 0;
}
