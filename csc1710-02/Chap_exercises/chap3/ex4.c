
#include<stdio.h>
int main (void)
{

   int F = 27, C;

   C = (F - 32) / 1.8;

   printf("If we take the number 27 in degrees Fahrenheit and change it to degrees Celcius we get: %i\n", C);

   return 0;
}
