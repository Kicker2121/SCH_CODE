#include<stdio.h>

int main (void)
{

   int x = 10, sum; 
   float y = 2.01;

   sum = (3.31 * x*-8) * (y) * (x*-7) / (7.16 * x*-6) + (y) * (x*-8);

   printf("When we evaluate the exponential expression we get: %d\n", sum);

   return 0;
}

